//! Audit verifier skeleton — spec §14.11.4 public erasure receipt + transparency log.
//!
//! A future release adds direct WAL chain-tip re-verification (HF3 fail-close) plus
//! the Ed25519 / Hybrid `chain_tip_signature` branch (GF5), Sigstore/Rekor
//! transparency-log verification, and the `arkhe-verify erasure-receipt` CLI.
